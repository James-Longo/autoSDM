% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extrapolate.R
\name{extrapolate}
\alias{extrapolate}
\title{Generate Niche Similarity Map}
\usage{
extrapolate(
  df,
  analysis_meta_path,
  output_dir = getwd(),
  scale = 10,
  coarse_meta_path = NULL,
  view = FALSE,
  gcs_bucket = NULL,
  wait = FALSE,
  zip = FALSE,
  python_path = NULL,
  gee_project = NULL
)
}
\arguments{
\item{df}{Data frame containing the original survey data (Latitude, Longitude) to determine the AOI.}

\item{analysis_meta_path}{Path to the JSON metadata file generated by `analyze_embeddings`.}

\item{output_dir}{Directory where the output map and metadata will be saved. Defaults to current working directory.}

\item{scale}{Resolution in meters for the output map. Defaults to 10.}

\item{coarse_meta_path}{Optional. Path to metadata for 1km filtering. If provided, analysis is restricted to 1km pixels > 5th percentile.}

\item{gcs_bucket}{Optional. GCS bucket name for server-side export. If provided, bypasses local download.}

\item{wait}{Logical. If TRUE, waits for the GCS export task(s) to complete and shows progress updates.}

\item{zip}{Logical. If TRUE, zips the output rasters into a single archive (local mode only).}
}
\value{
A list containing the paths to the generated map files, HTML, or GCS export details.
}
\description{
This function generates a continuous niche similarity map (dot product) in Google Earth Engine
based on the species centroid. The map is returned at the specified resolution.
}
\keyword{internal}
